{
  "name": "e-summarizer",
  "description": "A simple template with minimal dependencies",
  "version": "1.0.0",
  "scripts": {
    "dev": "deco dev",
    "deploy": "wrangler build && deco deploy -y ./dist",
    "gen": "deco gen > deco.gen.ts"
  },
  "dependencies": {
    "@deco/workers-runtime": "npm:@jsr/deco__workers-runtime@^0.5.2",
    "zod": "^3.24.3"
  },
  "devDependencies": {
    "wrangler": "^4.24.3"
  }
}
